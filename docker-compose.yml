services:
  experiment:
    hostname: exp
    image: davidedomini99/experiments-2025-aaai-sparsesul:1.0.1
    volumes:
      - davide.domini-volume:/data
    environment:
      DATA_OWNER: 1000:1000

volumes:
  davide.domini-volume:
    external: true
